#pragma once

// Напруга комірки через дільник на GPIO1.
//
// Повертає NAN, а не нуль, коли живлення не схоже на батарею: "не знаємо" це
// не "розряджено вщент". Заразом пише EV_VBAT_LOW / EV_VBAT_CRITICAL, але лише
// коли напруга справді відома — інакше вузол на USB щоцикл рапортував би
// критичний розряд неіснуючої батареї, і серед цього шуму справжня подія
// загубилась би.
float batteryVolts();
