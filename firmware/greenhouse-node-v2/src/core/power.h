#pragma once

#include <Arduino.h>

// Живлення периферії й засинання.

// Момент подачі Vext. Від нього рахується прогрів сенсорів і мітка `soil_at_ms`
// у пакеті — тобто це спільна нульова точка всього циклу.
//
// Ініціалізація I2C, SPI й радіо йде ПАРАЛЕЛЬНО з прогрівом, а не додається
// зверху: саме тому прогрів рахується від Vext, а не від кінця ініціалізації.
extern unsigned long vextOnAtMs;

void powerOnVext();
void powerOffVext();

// Приспати радіо, зняти Vext, відпустити піни й лягти так, щоб від старту до
// старту вийшло рівно SLEEP_MINUTES. Не повертається.
void enterDeepSleep();
