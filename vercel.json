{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo 'нема node-залежностей — пропускаємо'",
  "buildCommand": "uv venv .venv-build && uv pip install --python .venv-build -r requirements-docs.txt && .venv-build/bin/mkdocs build --strict",
  "outputDirectory": "site",
  "ignoreCommand": "git rev-parse HEAD^ >/dev/null 2>&1 && git diff --quiet HEAD^ HEAD -- documentation mkdocs.yml requirements-docs.txt vercel.json"
}
