"""
Абстрактний інтерфейс каналів сповіщень.

Кожна реалізація (Telegram, email, push і т.д.) живе в окремому модулі
цього пакета і імплементує метод send(alert).

Protocol використано замість ABC свідомо: Python duck typing вистачає,
не змушуємо конкретні класи наслідуватись від нашого класу - достатньо
щоб вони мали метод потрібної сигнатури.
"""

from typing import Protocol

from alerts import Alert


class Notifier(Protocol):
    """Інтерфейс каналу сповіщень."""

    def send(self, alert: Alert) -> None:
        """
        Надіслати алерт через відповідний канал.

        Не має підіймати виключення при мережевих або інших помилках зовнішнього
        сервісу - логувати і мовчки приймати. Один втрачений алерт краще
        за застряглий застосунок.
        """
